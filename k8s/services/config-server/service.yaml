apiVersion: v1
kind: Service
metadata:
  name: selmag-config-server-svc
  namespace: selmag
spec:
  type: ClusterIP
  selector:
    app: selmag-config-server
  ports:
    - name: http
      port: 8888
      targetPort: http
