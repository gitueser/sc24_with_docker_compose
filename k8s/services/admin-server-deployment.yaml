apiVersion: apps/v1
kind: Deployment
metadata:
  name: selmag-admin-server-deployment
  namespace: selmag
  labels:
    app: selmag-admin-server
spec:
  selector:
    matchLabels:
      app: selmag-admin-server
  template:
    metadata:
      labels:
        app: selmag-admin-server
    spec:
      containers:
        - name: selmag-admin-server
          image: host.docker.internal:5000/selmag/admin-server:24.1.1
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "cloudconfig"
#              value: "cloudconfig,observability"
            - name: SERVER_PORT
              value: "8085"
          envFrom:
            - configMapRef:
                name: selmag-config
            - secretRef:
                name: selmag-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: selmag-admin-server-svc
  namespace: selmag
spec:
  type: ClusterIP
  selector:
    app: selmag-admin-server
  ports:
    - name: http
      port: 8085
      targetPort: 8085
#---
#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: selmag-admin-server-ingress
#  namespace: selmag
#spec:
#  ingressClassName: nginx
#  rules:
#    - host: admin.selm.ag.192.168.49.2.nip.io
#      http:
#        paths:
#          - path: /
#            pathType: Prefix
#            backend:
#              service:
#                name: selmag-admin-server-svc
#                port:
#                  name: http
