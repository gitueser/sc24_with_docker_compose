apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: selmag-feedback-db-pvc
  namespace: selmag
  labels:
    app: selmag-feedback-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
