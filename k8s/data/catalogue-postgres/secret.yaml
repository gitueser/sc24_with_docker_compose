# Пока не используется: логин/пароль берём из selmag-config и selmag-secret через envFrom.
# Когда будешь переходить на best practice для БД:
# - вынесем POSTGRES_USER/POSTGRES_DB в отдельный ConfigMap БД
# - POSTGRES_PASSWORD в отдельный Secret БД
# - уберём лишнее из общего selmag-secret
