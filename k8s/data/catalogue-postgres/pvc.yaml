apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: selmag-catalogue-db-pvc
  namespace: selmag
  labels:
    app: selmag-catalogue-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
