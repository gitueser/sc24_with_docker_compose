{{- if .Values.bootstrap.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "selmag.configMapName" . }}
data:
  {{- $raw := .Files.Get "files/.env.config" | default "" -}}
  {{- if eq $raw "" }}
_error: "files/.env.config is missing in the chart"
  {{- else }}
  {{- range $line := splitList "\n" $raw }}
  {{- $l := trim $line }}
  {{- if and (ne $l "") (not (hasPrefix "#" $l)) }}
  {{- $parts := splitList "=" $l }}
  {{- if ge (len $parts) 2 }}
  {{ index $parts 0 }}: {{ join "=" (slice $parts 1) | quote }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- end }}
  {{- end }}
