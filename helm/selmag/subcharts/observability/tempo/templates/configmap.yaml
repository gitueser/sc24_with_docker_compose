apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.config.configMapName }}
  labels:
    {{- include "tempo.labels" . | nindent 4 }}
data:
  {{ .Values.config.fileName }}: |
{{- .Values.tempoConfigYaml | nindent 4 }}
