management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - prometheus
  tracing:
    enabled: true
    sampling:
      probability: 0.1
  zipkin:
    tracing:
      endpoint: ${TEMPO_ZIPKIN_ENDPOINT:http://tempo:9411/api/v2/spans}
